import React, { useState } from "react";

interface useOpenModalProps {
  isModalOpen: boolean;
  animeId: string;
}

const useOpenModal = ({ isModalOpen, animeId }: useOpenModalProps) => {};
export default useOpenModal;
